package scenes;

public class DefaultScene extends AbstractScene {
    /**
     * Constructs an instance of AbstractScene with the specified name.
     *
     * @param name the name of the scene to be created
     */
    public DefaultScene(String name) {
        super(name);
        System.out.printf("Default Scene has been created%n");
    }

}
